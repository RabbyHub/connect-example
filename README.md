# Rabby Wallet connect example

### Running examples

To run an example locally, install dependencies.

```bash
pnpm install
```

Then go into an example directory, eg: `rainbow`.

```bash
cd examples/rainbow
```

Then run the dev script.

```bash
pnpm run dev
```